<div>
  <span>*必填欄位</span>
  <span>請填寫支出紀錄</span>
</div>
<div class="container">
  <div class="row">
    <div class="col">
      <form action="/expenses/new" method="POST">
        <label for="name">名稱*:</label>
        <input type="text" name="name" id="name" palceholder="支出名稱" required>
        <label for="date">日期*:</label>
        {{!-- safari doesn't support type="date" --}}
        <input type="text" name="date" id="date" palceholder="支出日期 e.g.格式: YYYY/MM/DD" 
              pattern="\s*[0-9]{4}\/([1][0-2]|[0][1-9])\/([0][1-9]|[1-2][0-9]|[3][0-1])\s*" 
              title="請符合: YYYY/MM/DD" required>
        <label for="category">類別*:</label>
        <select name="category" id="category" required>
          <option value="" selected disabled>類別</option>
          {{#each categories}}
          <option value="{{name}}">{{name}}</option>
          {{/each}}
        </select>
        <label for="amount">金額*:</label>
        <input type="number" name="amount" id="amount" palceholder="支出金額" required>
        <button type="submit">新增支出紀錄</button>
      </form>
    </div>
  </div>
</div>